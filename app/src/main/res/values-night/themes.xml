<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ChatMatch" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/customColor</item>
        <item name="colorPrimaryVariant">@color/customColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/customColor</item>
        <item name="colorSecondaryVariant">@color/customColor</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:actionBarStyle" tools:targetApi="l">@color/customColor</item>
        <item name="android:textColor">#FFFFFF</item>
        <!-- Customize your theme here. -->
    </style>
</resources>